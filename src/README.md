Local development
-----------------

To launch Postgres and Redis for local development `docker-compose-development.yml` run:

```
make dev_docker
```

Run Django:

```
make run
```

Run Celery:
```
make celery
```
